/**
 * Global configuration settings for the application.
 */
export const CONFIG = {
    /** @type {boolean} Determines if the marketplace features (catalog, cart) are enabled. */
    enableMarketplace: false,
};
